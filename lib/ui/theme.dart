import 'dart:ui';

const buttonsSpacing = 16.0;
const modalSheetHeight = 150.0;

const tshirtSize = Size(671, 675);
const printSize = Size(297, 210);
const printOffset = Offset(187.5, 127.19);
const printOffsetFromCenter = Offset(0, -105);
